# Fleetr

a small utility focused on maintaining status awareness on reachable nodes (aka your fleet + rust).

# Usage

- [ ] fleet populate ssh config
- [ ] ssh config file is in default location (/home/$USER/.ssh/config)
- [ ] all desired hosts are reachable

# Current State

Only takes battery status and mainly just for laptops. will add support for my all in one HP later.
